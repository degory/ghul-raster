# shell install scripts
**/*.run

# .NET objects
obj/
bin/
nupkg/

**/TestResults
**/*.runtimeconfig.json
**/*.il
**/*.dll
**/*.exe
**/*.exe.mdb
**/*.vsix
.assemblies.json
output.txt
.vs/*

# images a test wrote; the .expected beside them is what is asserted, and
# images/ holds the ones this repository keeps rather than any it produced
*.png
!*.png.expected
!**/images/*.png

# animations a test wrote, as for images above
*.gif
!*.gif.expected
