# Scope is deliberately narrow - NOT a `*` catch-all, which would require
# code-owner review on every PR. The workflows and this file are gated so the
# release machinery can't change unreviewed.
/.github/workflows/   @degory
/.github/CODEOWNERS   @degory
